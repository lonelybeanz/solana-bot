// Code generated by jzero. DO NOT EDIT.
package handler

import (
	"net/http"
	"time"

	"solana-bot/internal/svc"

	"github.com/zeromicro/go-zero/rest"

	version "solana-bot/internal/handler/version"
)

var (
	_ = http.StatusOK
	_ = time.Now()
)

func RegisterHandlers(server *rest.Server, serverCtx *svc.ServiceContext) {
	{
		server.AddRoutes(
			[]rest.Route{
				{
					Method:  http.MethodGet,
					Path:    "/version",
					Handler: version.GetVersion(serverCtx),
				},
			},
			rest.WithPrefix("/api/v1"),
		)
	}
	{
		server.AddRoutes(
			[]rest.Route{
				{
					Method:  http.MethodGet,
					Path:    "/pumpBot",
					Handler: version.PumpBot(serverCtx),
				},
			},
			rest.WithPrefix("/api/v1"),
		)
	}

}
